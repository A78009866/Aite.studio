name: Aite Studio APK Builder

on:
  repository_dispatch:
    types: [build-apk]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Prepare Gradle 8.2 & Assets
        run: |
          # 1. إنشاء المجلدات
          mkdir -p gradle/wrapper app/src/main/res/mipmap-mdpi app/src/main/res/xml
          
          # 2. تحميل المحرك
          curl -L https://raw.githubusercontent.com/gradle/gradle/v8.2.0/gradle/wrapper/gradle-wrapper.jar -o gradle/wrapper/gradle-wrapper.jar
          
          # 3. إعداد الخصائص لنسخة 8.2
          cat <<EOF > gradle/wrapper/gradle-wrapper.properties
          distributionBase=GRADLE_USER_HOME
          distributionPath=wrapper/dists
          distributionUrl=https\://services.gradle.org/distributions/gradle-8.2-bin.zip
          networkTimeout=10000
          validateDistributionUrl=true
          zipStoreBase=GRADLE_USER_HOME
          zipStorePath=wrapper/dists
          EOF

          # 4. إنشاء ملف XML الضروري للملفات
          cat <<EOF > app/src/main/res/xml/file_paths.xml
          <?xml version="1.0" encoding="utf-8"?>
          <paths>
              <external-path name="external_files" path="." />
          </paths>
          EOF

          # 5. حقن بيانات المستخدم وتوحيد إصدار الجافا (JVM Target)
          # استبدال اسم التطبيق والحزمة
          sed -i "s/APP_NAME_PLACEHOLDER/${{ github.event.client_payload.app_name }}/g" app/src/main/AndroidManifest.xml
          sed -i "s/APP_PACKAGE_NAME/${{ github.event.client_payload.package_name }}/g" app/build.gradle
          
          # توحيد إصدار الجافا إلى 17 ليتوافق مع الكوتلن (حل المشكلة الأخيرة)
          sed -i "s/JavaVersion.VERSION_1_8/JavaVersion.VERSION_17/g" app/build.gradle
          sed -i "s/jvmTarget = '1.8'/jvmTarget = '17'/g" app/build.gradle
          
          # حقن رابط الموقع
          sed -i "s|WEB_URL_PLACEHOLDER|${{ github.event.client_payload.app_url }}|g" app/src/main/java/com/aite/studio/MainActivity.kt
          
          # 6. معالجة الأيقونة
          echo "${{ github.event.client_payload.icon_data }}" | cut -d',' -f2 | base64 -d > app/src/main/res/mipmap-mdpi/ic_launcher.png

      - name: Build APK
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: build-${{ github.run_number }}
          name: Build #${{ github.run_number }} - ${{ github.event.client_payload.app_name }}
          files: app/build/outputs/apk/debug/app-debug.apk
          body: "تم بناء التطبيق بنجاح عبر Aite.studio"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Send to Telegram
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            ✅ مبروك! البناء اكتمل بنجاح.
            📦 اسم التطبيق: ${{ github.event.client_payload.app_name }}
            🔗 رابط التحميل: https://github.com/${{ github.repository }}/releases/download/build-${{ github.run_number }}/app-debug.apk
          document: app/build/outputs/apk/debug/app-debug.apk
