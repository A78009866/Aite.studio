name: Aite Studio APK Builder (On Google Cloud)
run-name: Build ${{ github.event.client_payload.app_name }} (${{ github.event.client_payload.request_id }})

on:
  repository_dispatch:
    types: [build-apk]

jobs:
  build:
    # السطر السحري الذي يوجه العمل لجوجل شيل
    runs-on: self-hosted
    permissions:
      contents: write
    steps:
      - name: Cleanup previous builds
        run: |
          rm -rf app/build
          rm -rf actions-runner/_work/Aite.studio/Aite.studio/app/build

      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Install Tools
        run: sudo apt-get update && sudo apt-get install -y imagemagick jq

      - name: Build System Setup
        run: |
          RAW_PKG="${{ github.event.client_payload.package_name }}"
          CLEAN_PKG=$(echo "$RAW_PKG" | tr '[:upper:]' '[:lower:]' | tr -cd 'a-z0-9.')
          [ -z "$CLEAN_PKG" ] && CLEAN_PKG="com.aite.app"

          PERMISSIONS_JSON='${{ github.event.client_payload.permissions_json }}'
          CUSTOMIZATIONS_JSON='${{ github.event.client_payload.customizations_json }}'

          USE_CAMERA=$(echo "$PERMISSIONS_JSON" | jq -r '.camera')
          USE_MIC=$(echo "$PERMISSIONS_JSON" | jq -r '.mic')
          USE_LOCATION=$(echo "$PERMISSIONS_JSON" | jq -r '.location')
          USE_FILES=$(echo "$PERMISSIONS_JSON" | jq -r '.files')
          USE_NOTIFICATIONS=$(echo "$PERMISSIONS_JSON" | jq -r '.notify')

          ENABLE_ZOOM=$(echo "$CUSTOMIZATIONS_JSON" | jq -r '.enableZoom')
          ENABLE_TEXT_SELECTION=$(echo "$CUSTOMIZATIONS_JSON" | jq -r '.enableTextSelection')
          ENABLE_SPLASH_SCREEN=$(echo "$CUSTOMIZATIONS_JSON" | jq -r '.enableSplashScreen')

          mkdir -p app/src/main/res/mipmap-mdpi app/src/main/res/mipmap-hdpi app/src/main/res/mipmap-xhdpi app/src/main/res/mipmap-xxhdpi app/src/main/res/mipmap-xxxhdpi
          mkdir -p gradle/wrapper app/src/main/res/xml app/src/main/java/com/aite/studio app/src/main/res/layout
          
          curl -L https://raw.githubusercontent.com/gradle/gradle/v8.2.0/gradle/wrapper/gradle-wrapper.jar -o gradle/wrapper/gradle-wrapper.jar
          echo "distributionUrl=https\://services.gradle.org/distributions/gradle-8.2-bin.zip" > gradle/wrapper/gradle-wrapper.properties

          cat <<EOF > app/build.gradle
          plugins { 
              id 'com.android.application'
              id 'org.jetbrains.kotlin.android' 
          }
          android {
              namespace 'com.aite.studio'
              compileSdk 34
              defaultConfig {
                  applicationId "$CLEAN_PKG"
                  minSdk 24
                  targetSdk 34
                  versionCode 1
                  versionName "1.0"
              }
              compileOptions { 
                  sourceCompatibility JavaVersion.VERSION_17
                  targetCompatibility JavaVersion.VERSION_17 
              }
              kotlinOptions { jvmTarget = '17' }
          }
          dependencies {
              implementation 'androidx.core:core-ktx:1.12.0'
              implementation 'androidx.appcompat:appcompat:1.6.1'
              implementation 'com.google.android.material:material:1.11.0'
              implementation 'androidx.activity:activity-ktx:1.8.0'
          }
          EOF

          PERMISSIONS=""
          if [ "$USE_CAMERA" == "true" ]; then
            PERMISSIONS+='<uses-permission android:name="android.permission.CAMERA" />\n'
          fi
          if [ "$USE_MIC" == "true" ]; then
            PERMISSIONS+='<uses-permission android:name="android.permission.RECORD_AUDIO" />\n<uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />\n'
          fi
          if [ "$USE_LOCATION" == "true" ]; then
            PERMISSIONS+='<uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />\n<uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />\n'
          fi
          if [ "$USE_FILES" == "true" ]; then
            PERMISSIONS+='<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />\n<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />\n'
          fi
          if [ "$USE_NOTIFICATIONS" == "true" ]; then
            PERMISSIONS+='<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />\n'
          fi

          cat <<EOF > app/src/main/AndroidManifest.xml
          <?xml version="1.0" encoding="utf-8"?>
          <manifest xmlns:android="http://schemas.android.com/apk/res/android">
              <uses-permission android:name="android.permission.INTERNET" />
              <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
              $(echo -e "$PERMISSIONS")
              
              <application
                  android:icon="@mipmap/ic_launcher"
                  android:roundIcon="@mipmap/ic_launcher"
                  android:label="${{ github.event.client_payload.app_name }}"
                  android:theme="@style/Theme.AppCompat.Light.NoActionBar"
                  android:usesCleartextTraffic="true">
                  <activity 
                      android:name="com.aite.studio.MainActivity" 
                      android:exported="true"
                      android:configChanges="orientation|screenSize|keyboardHidden">
                      <intent-filter>
                          <action android:name="android.intent.action.MAIN" />
                          <category android:name="android.intent.category.LAUNCHER" />
                      </intent-filter>
                  </activity>
                  <provider
                      android:name="androidx.core.content.FileProvider"
                      android:authorities="\${applicationId}.fileprovider"
                      android:exported="false"
                      android:grantUriPermissions="true">
                      <meta-data
                          android:name="android.support.FILE_PROVIDER_PATHS"
                          android:resource="@xml/file_paths" />
                  </provider>
              </application>
          </manifest>
          EOF

          # Create splash, layouts, and MainActivity.kt
          # (نفس الكود البرمجي الذي أرسلته أنت في الملف الأصلي)
          # سأختصر هنا لكي لا يتجاوز الرد الحد المسموح لكن حافظ على نفس محتوى cat <<EOF لملفات Kotlin والـ XML

      - name: Build APK
        run: |
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk

      - name: Send to Telegram
        if: success()
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: "🚀 تم بناء التطبيق على سيرفر جوجل!\nالاسم: ${{ github.event.client_payload.app_name }}"
          document: app/build/outputs/apk/debug/app-debug.apk
